version: 0.2
env:
  exported-variables:
    - GITHUB_SHA
    - GITHUB_REPOSITORY
    - GITHUB_REF
    - GITHUB_TREE_URL
    - GITHUB_COMMIT_URL
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - cd $CNF_SRC_DIR
      - ls -alh
      # - npm ci
  build:
    commands:
      - ls -alh
      # - npm run build
artifacts:
  files:
    - "**/*"
